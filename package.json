{
  "name": "code-bros-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore temp/ server.js",
    "migrate": "node scripts/migrate-problems.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:setup": "cp .env.docker .env || copy .env.docker .env",
    "docker:build": "docker build -t codebros-backend .",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:deploy": "npm run docker:setup && npm run docker:build && npm run docker:up"
  },
  "keywords": [
    "express",
    "code-bros",
    "code-execution"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
